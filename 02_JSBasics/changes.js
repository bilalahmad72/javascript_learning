let gameName = "Skill Create";

// Its will override the value that initialized before
gameName = "Spider Man";
console.log(gameName);

const username = "Bilal Ahmad";

// in const we can override the value, if its initialized first time, then can't be change
username = "Bilal";

console.log(username);
